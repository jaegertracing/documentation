name: jaeger-all-in-one completion fish
synopsis: Generate the autocompletion script for fish
description: |
    Generate the autocompletion script for the fish shell.

    To load completions in your current shell session:

    	jaeger-all-in-one completion fish | source

    To load completions for every new session, execute once:

    	jaeger-all-in-one completion fish > ~/.config/fish/completions/jaeger-all-in-one.fish

    You will need to start a new shell for this setup to take effect.
usage: jaeger-all-in-one completion fish [flags]
options:
    - name: help
      shorthand: h
      default_value: "false"
      usage: help for fish
    - name: no-descriptions
      default_value: "false"
      usage: disable completion descriptions
see_also:
    - jaeger-all-in-one completion - Generate the autocompletion script for the specified shell
