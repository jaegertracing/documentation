name: jaeger-all-in-one completion powershell
synopsis: generate the autocompletion script for powershell
description: |2

  Generate the autocompletion script for powershell.

  To load completions in your current shell session:
  PS C:\> jaeger-all-in-one completion powershell | Out-String | Invoke-Expression

  To load completions for every new session, add the output of the above command
  to your powershell profile.
usage: jaeger-all-in-one completion powershell [flags]
options:
- name: help
  shorthand: h
  default_value: "false"
  usage: help for powershell
- name: no-descriptions
  default_value: "false"
  usage: disable completion descriptions
see_also:
- jaeger-all-in-one completion - generate the autocompletion script for the specified
  shell
